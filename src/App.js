import { BrowserRouter, Route, Switch} from "react-router-dom"
import './App.css';

function App() {
  return (
    <BrowserRouter>
        <Switch>
            <Route component={} path='' />
        </Switch>
    </BrowserRouter>
  );
}

export default App;
